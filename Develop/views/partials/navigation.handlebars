<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="{{linkUrl}}">
                {{linkLabel}}
                {{!-- if else based on login status --}}
            </a>
        </div>
    </div>
</nav>